-- 기록 입력칸에 미리 채워 넣을 양식(빈칸 서식). 운동 직후 빈 textarea를 마주하면 무엇을 어떻게
-- 적어야 할지 막막해서 기록이 잘 안 써진다는 실사용 피드백에서 나왔다.
--
-- 세션마다 한 번만 생성해 재사용한다(기록칸을 열 때마다 LLM을 부르면 매번 몇 초씩 기다려야 한다).
-- 와드 원문이 수정되면 양식도 더 이상 맞지 않으므로 NULL로 비우고 다음에 다시 만든다.
ALTER TABLE wod_sessions ADD COLUMN record_template TEXT;
