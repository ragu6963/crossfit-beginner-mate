# Design — CrossFit Beginner Mate

크로스핏 입문 1주차 사용자를 위한 "오늘의 와드 확인 + 상세 가이드" 앱과, 와드/기록을 관리하는 관리자 대시보드의
잠긴 디자인 시스템. 이후 이 프로젝트에서 화면을 손볼 때는 이 파일을 먼저 읽고, 새로 만들지 말고 확장한다.

**2026-08-12 개정** — 사용자 요청으로 이전 Hum(따뜻한 크림) 시스템을 **Sport**(레드+블랙, 강렬한 운동 에너지)로
전면 교체했다. 이 파일이 유일한 소스이며, 아래 내용이 이전 판을 대체한다.

**2026-08-12 재개정 — 페이퍼 밴드 다크 → 라이트.** 다크는 Sport 테마의 필수 요건이 아니라 "강렬함"을
표현하기 위한 선택이었다. 사용자가 라이트 배경을 요청해 페이퍼 밴드만 뒤집었다 — 레드 악센트, 콘덴스드
디스플레이, 슬랩 반경, 빠른 모션 등 Sport의 나머지 정체성은 그대로다. 아래 팔레트 절이 최신값이다.

**2026-08-12 3차 수정 — 보조색(danger/info/cool) 채도 상향.** 라이트 전환 때 대비(4.5:1)를 맞추려고
명도만 낮추고 채도(chroma)는 0.12~0.15로 낮게 뒀더니 칙칙하고 흐릿해 보였다("구리구리하다" 피드백).
명도는 유지하되 채도를 0.18~0.20으로 올려 더 선명하게 고쳤다 — danger는 앰버-브라운에서 채도 높은
세이프티 오렌지로, info/cool도 더 선명한 블루/바이올렛으로. 대비는 그대로 유지된다(채도만 변경, 명도는
거의 동일).

**2026-08-12 4차 수정 — 페이퍼를 순백(#fff)으로.** color.md의 기본 원칙은 "순백을 배경으로 쓰지 말 것"
(너무 flat/synthetic해 보인다는 이유)이지만, 사용자가 명시적으로 순백을 요청해 이 휴리스틱을 오버라이드
한다. `--color-paper`만 `#ffffff`로 바꾸고, 카드/버튼 표면 단계(paper-2/3/surface-raised)는 계속 레드
악센트와 같은 hue(25)로 톤을 살짝 얹어 순백 배경 위에서도 붕 뜨지 않게 했다.

**2026-08-12 5차 수정 — 가이드 섹션의 side-stripe 제거.** 가이드 섹션(`.guide-section`)과 인라인 팁 노트
(초보자 팁/주의사항/Scaling)에 카테고리 색을 **왼쪽 보더 스트라이프**로 넣었는데, 이건 Hallmark 자체
`anti-patterns.md`가 "The side-stripe card"라는 이름으로 명시적으로 금지하는 패턴이다("두꺼운 컬러
보더 한쪽 edge, 특히 left — 매우 2018-SaaS-AI스러움"). 사용자 지적으로 발견해 고쳤다:
- `.guide-section` — `border-left` 삭제, 대신 전체 헤어라인 보더(`border: 1px solid`)로 카드 경계를 표현.
  카테고리 색은 섹션 타이틀 옆의 **작은 정사각형**(`::before`, `background: currentColor`)으로 이동.
- 인라인 팁 노트 — `border-left` 삭제. 카테고리 색은 이미 있던 **볼드 라벨 텍스트 색**만으로 표현(라벨
  자체가 충분히 스캔 가능해서 보더가 없어도 정보 손실 없음).
동일한 "장식이 아니라 정보 위계" 목적은 유지하되, 표현 방식만 anti-patterns.md가 허용하는 대안
("헤어라인 전체 보더, 무테, 또는 헤딩 옆 작은 accent square")으로 바꿨다.

**2026-08-12 6차 수정 — paper-2/paper-3/surface-raised 톤 계단 전면 폐기.** 사용자가 각 섹션 카드의
배경색(웜틴트 그레이 계단)을 계속 마음에 안 들어 했다 — 명도를 낮춰도, 채도를 조정해도 "탁하다"는
인상이 반복됐다. 근본 원인은 색 자체가 아니라 **배경색으로 위계를 표현하는 방식 자체**였다고 보고,
표면 색 계단을 통째로 없앴다: 페이지·카드·모달·가이드 섹션 전부 순백(`--color-paper`) 하나만 쓰고,
위계는 전적으로 **보더**(헤어라인 `--color-hairline` = 콘텐츠 카드용, 더 진한 `--color-line` = 버튼용)와
**반투명 잉크 워시**(`--color-wash` / `--color-wash-strong` = hover/press 피드백용)로만 표현한다.
브루탈/스포츠 계열 원래 문법(그림자보다 보더, 색보다 무게)에 오히려 더 충실해졌다. 아래 팔레트 절이
최신값이다 — paper-2/paper-3/surface-raised 토큰은 더 이상 존재하지 않는다.

**2026-08-13 7차 수정 — 가이드 섹션 카드 박스 제거.** "각 섹션을 카드로 가둬서 답답하다"는 피드백으로
`.guide-section`에서 `border`(전체 테두리)·`padding`(좌우)·`border-radius`를 모두 뺐다. 대신 섹션마다
위에 헤어라인 구분선 하나만 두는 평문 리듬으로 바꿨다(`.guide-movement + .guide-movement`에 이미 쓰던
관례와 동일 — 첫 섹션은 구분선 없음). 카테고리 색은 여전히 타이틀 옆 작은 정사각형 + 라벨 색으로만
표현되고, 이번 수정으로 바뀐 건 아니다.

**2026-08-13 8차 수정 — 섹션 구분선과 동작 구분선의 굵기 분리.** 7차 수정으로 섹션 사이 구분선(1px
solid)과 동작 사이 구분선(당시 1px solid)이 같은 무게라 위계가 안 읽혔다. 두 단계를 시각적으로 분리:
섹션 구분선은 그대로 1px solid 헤어라인(상위 레벨, 얇게), 동작(`.guide-movement + .guide-movement`)
구분선은 `2px dotted`로 바꿔 하위 레벨임을 굵기+스타일 둘 다로 드러냈다.

**2026-08-13 9차 수정 — 동작 구분선을 실제 요소 + 굵은 점 + 중앙 사각형으로.** border 기반 dotted는
여전히 "딱히 구분되지 않는다"는 피드백을 받았다(브라우저 dotted 렌더링은 점이 가늘고 흐릿하다). border
대신 `shared.js`의 `renderMovementsHtml`이 동작 사이에 실제 `<div class="guide-movement-divider">■</div>`
요소를 끼워 넣도록 바꾸고(첫 동작 앞에는 없음), CSS로 flex 양옆에 굵은 점 라인(`radial-gradient`
배경, 반지름 3.5px)을 두고 가운데 실제 텍스트 콘텐츠 "■"(15px, `--color-ink` 풀강도)를 둬 "점 · · · ■
· · · 점" 모양을 만들었다. 이 사각형은 `.guide-section-title` 옆 사각형과 같은 글리프를 재사용한 것 —
"같은 마크의 한 단계 아래 버전"으로 읽히게 하기 위함이다.

**2026-08-13 10차 수정 — 구분선 자체를 폐기, 번호 인덱스로 교체.** 9차 수정의 점+사각형 구분선은
"효과는 있지만 못생겼다"는 피드백을 받았다. 라인을 억지로 굵게/화려하게 만드는 대신, 애초에 라인이
필요 없는 방식으로 바꿨다 — 동작은 실제로 순서가 있는 콘텐츠이므로 번호 인덱스(01/02/03…)를 붙이면
장식이 아니라 정보다. `shared.js`의 `renderMovementsHtml`이 각 동작을 `.guide-movement`(flex row) =
`.guide-movement-index`(콘덴스드 2자리 번호, `--font-outlier`, 날짜 숫자·스코어 배지와 같은 어휘) +
`.guide-movement-body`(이름/설명/노트)로 렌더링한다. `.guide-movement-divider` 요소/CSS는 완전히
제거했다.

## Genre
**editorial** — Hallmark 카탈로그에서 Sport는 editorial 클러스터에 속한 테마다. 다만 이 앱은 여전히
마케팅 페이지가 아니라 매일 여는 개인용 유틸리티라, editorial의 "차분한 에디토리얼" 기본 보이스 대신
Sport 특유의 "경기장/스코어보드" 에너지를 그대로 살렸다.

## 매크로구조에 대한 의도적 이탈 (변경 없음)

Hallmark의 21개 매크로구조는 전부 마케팅 랜딩페이지 모양이라 이 앱(단일 화면 유틸리티)에는 그대로
적용하지 않는다. 이전 판과 동일하게 앱 전용 구조를 쓴다:

- **User pages:** Day-Rail + Session List / Detail Sheet
- **Admin pages:** 같은 골격 + CRUD 오버레이(세션 추가 모달, 상세 시트 안 관리 액션 + 내 기록 입력)

## 테마 — Sport(어댑트)

카탈로그의 **Sport**(레드 악센트 · 콘덴스드 디스플레이 · 오버사이즈 솔리드 CTA · 빠른 모션)를 적용하되,
Hallmark의 최신 전역 규칙과 충돌하는 두 지점은 의도적으로 고쳤다:

- **이탤릭 → 로만.** Sport 원 스펙은 디스플레이 헤딩에 이탤릭을 쓰지만, 전역 타이포 규칙("헤딩은 항상
  roman, 이탤릭 헤딩은 가장 흔한 AI 티")이 테마 스펙보다 우선한다. 강조는 굵기 900 + 자간 + 레드 악센트로
  대신한다.
- **Inter Tight → Big Shoulders Display.** Inter Tight는 typography.md에서 "디스플레이 금지, 기술 테마의
  바디 폴백만 허용"으로 명시돼 있어 Sport의 디스플레이 페이스로 쓸 수 없다. 대신 typography.md가 스포츠
  용도로 직접 추천하는 **Big Shoulders Display**(산업용 콘덴스드)를 아웃라이어로 쓴다.
- **한글 커버리지.** Big Shoulders Display / Pretendard 모두 라틴 전용이므로, 본문·헤딩의 한글은 계속
  Pretendard가 맡는다. Big Shoulders Display는 숫자·워드마크 같은 라틴/숫자 전용 자리에만 쓴다
  (아웃라이어 ≤2 슬롯 규칙 — "숫자/짧은 라벨"이라는 하나의 역할로 일관되게 사용).
- **멀티 파스텔 밴드 → 카드 + 컬러 보더.** 이전(Hum) 판에서 가이드 섹션마다 파스텔 배경 밴드를 썼는데,
  전체 배경 풀필보다 정보 위계를 더 또렷하게 전달하는 쪽을 택해 **왼쪽 컬러 보더 + 라벨 색**으로
  구현했다(장식이 아니라 여전히 스캐닝 보조 장치). 페이퍼 밴드를 라이트로 바꾼 뒤에도 이 방식은
  그대로 유지한다 — 파스텔 배경 밴드로 되돌리지 않는다.
- **소프트 섀도 → 하드 보더.** 브루탈/스포츠는 라운드보다 슬랩(각진 면), 그림자보다 보더가 기본
  문법이다(layout-and-space.md § Depth — "무게와 크기가 그림자보다 나은 위계 장치"). 페이퍼 밴드가
  다크였을 때는 "다크 배경 위 컬러 글로우 섀도" 금지 규칙(anti-patterns.md)도 겹쳐서 섀도를 뺐는데,
  라이트로 바뀐 지금도 그래픽/포스터 느낌을 위해 보더 기반 표현을 그대로 유지한다. 카드 리프트는
  그림자 대신 보더 두께/색 변화로 표현한다.
- **버튼 표면 단계 분리 (2026-08-12 버그 수정).** `.btn-secondary`가 `--color-paper-3`를 배경으로 썼는데,
  이 버튼이 놓이는 `.guide-section`(예: "내 기록" 섹션)도 같은 토큰을 배경으로 써서 버튼이 배경에
  묻히는 문제가 있었다. 버튼 전용 `--color-surface-raised` 토큰을 한 단계 더 분리해 추가하고, 모든
  `.btn-secondary` / `.detail-close-btn`에 헤어라인 보더를 붙여 어떤 표면 위에 있어도 항상 구분되게
  했다.

## 색상 팔레트 (OKLCH)

```css
--color-paper:        #ffffff;               /* 순백 — 유일한 표면색. 페이지·카드·모달·섹션 전부 이거 하나 */
--color-ink:          oklch(18% 0.012 25);   /* 거의 검정 — 절대 #000 아님 */
--color-ink-2:        oklch(18% 0.012 25) / 72%;
--color-ink-3:         oklch(18% 0.012 25) / 46%;
--color-hairline:     oklch(18% 0.012 25) / 18%;  /* 콘텐츠 카드 보더 */
--color-line:          oklch(18% 0.012 25) / 55%;  /* 버튼 보더 — 헤어라인보다 진해서 클릭 가능함이 드러남 */
--color-wash:          oklch(18% 0.012 25) / 6%;   /* hover 틴트 */
--color-wash-strong:   oklch(18% 0.012 25) / 10%;  /* active/press 틴트 */

--color-accent:        oklch(54% 0.22 25);   /* 시그널 레드 — 브랜드 프라이머리 (라이트 배경용, 다크판보다 진함) */
--color-accent-deep:   oklch(42% 0.20 25);   /* 버튼 눌림/에지 */
--color-accent-ink:    oklch(98% 0.006 25);  /* 레드 위 텍스트 */

--color-danger:        oklch(56% 0.20 45);   /* 세이프티 오렌지 — 레드(브랜드)와 구분되는 파괴적 액션 색 */
--color-info:          oklch(50% 0.19 235);  /* 선명한 블루 — 전략/팁 카테고리 전용 */
--color-cool:          oklch(54% 0.18 300);  /* 선명한 바이올렛 — 쿨다운 카테고리 전용 */

--color-focus:         oklch(62% 0.19 95);   /* 골드 포커스 링 — 라이트 배경에서도 3:1 이상 대비 */
--color-scrim:         oklch(8% 0.01 25 / 0.6);  /* 모달/시트 백드롭 — 다크·라이트 공통 */
```

색 사용 원칙: 배지·칩은 파스텔 풀필이 아니라 **표면 색 + 컬러 텍스트/보더 아웃라인**으로 통일한다.
본문(ink on paper), 레드 텍스트, 앰버 텍스트 모두 4.5:1 이상 확인.

## 타이포그래피

- **Body / Heading (한글):** Pretendard — body 400, heading 800(Black 없으면 ExtraBold), 자간 살짝 좁힘.
  로만체만 사용(이탤릭 없음).
- **Outlier (숫자·워드마크 전용, 라틴/숫자 자리에만):** Big Shoulders Display 800 — 올캡스, 콘덴스드.
  워드마크, 날짜 셀 숫자, 스코어 배지에 사용. `font-variant-numeric: tabular-nums` 병행.
- 세리프 없음.

## 타입 스케일 (변경 없음)

```css
--text-xs: 0.8125rem;  --text-sm: 0.875rem;  --text-base: 1rem;
--text-md: 1.0625rem;  --text-lg: 1.25rem;   --text-xl: 1.5rem;
--text-2xl: 1.875rem;
```

## 스페이싱 (변경 없음)

```css
--space-3xs: 0.25rem; --space-2xs: 0.5rem; --space-xs: 0.75rem;
--space-sm: 1rem;     --space-md: 1.5rem;  --space-lg: 2rem;
--space-xl: 3rem;     --space-2xl: 4rem;
```

## 반경 / 표면

브루탈/스포츠는 라운드가 아니라 슬랩(각진 면)이 기본이다:

```css
--radius-card: 6px; --radius-input: 4px; --radius-chip: 4px; --radius-pill: 6px;
```

날짜 셀 숫자만 예외로 완전 원(`border-radius: 50%`) 유지 — 저지 등번호/스코어보드 관용구.
카드는 `box-shadow` 없이 순백 배경 + 1px 헤어라인 보더로 표현하고, hover/press 시 보더 색만 레드로
바뀐다(두께·배경색은 고정 — 6차 수정 이후 배경색으로 상태를 표현하지 않는다).

## 모션

```css
--ease-out: cubic-bezier(0.16, 1, 0.3, 1);
--ease-press: cubic-bezier(0.2, 0.7, 0.3, 1);
--dur-micro: 90ms; --dur-short: 160ms; --dur-long: 280ms;   /* Sport = 0.7배속, sharp ease-out */
```

- 버튼: hover에 살짝 뜨고, active에 눌려서 내려감 — 스포츠 테마답게 이전보다 더 빠르고 스냅있게.
- 카드: hover/press 시 보더가 레드로 두꺼워짐(그림자 아님).
- **시그니처 모먼트 — 가로 스윕 언더라인.** 날짜 타이틀 아래 레드 룰이 페이지 로드/날짜 이동 시
  `scaleX(0 → 1)`로 한 번 쓸어 들어온다(Sport의 "horizontal sweep" 리빌을 유일한 오케스트레이션 모먼트로
  채택 — 스크롤마다 반복하지 않음).
- `prefers-reduced-motion: reduce` — 모든 공간 이동을 150ms opacity 크로스페이드로 축소, 스윕은 즉시 표시.

## 마이크로인터랙션 원칙 (변경 없음)

- 조용한 성공 유지. 파괴적 액션은 `confirm()` 유지. 포커스 링은 즉시 표시(앰버, 3:1 이상 대비).

## CTA 보이스

- **Primary(오버사이즈 솔리드):** 레드 풀필, 흰 텍스트, 각진 슬랩 반경. 화면당 핵심 순간에 1개만.
- **Secondary:** 순백 배경 + 진한 보더(`--color-line`), hover 시 워시 틴트(`--color-wash`) — 보더
  자체가 진해서 콘텐츠 카드(헤어라인)와 항상 구분된다.
- **Tertiary/outline:** 헤어라인 보더 + 텍스트만.
- **Danger:** 앰버 아웃라인(레드=브랜드와 구분하기 위해 델리트류는 앰버를 쓴다) — push 버튼 아님.

## 가이드 섹션 카테고리 색

목표=레드 / 웜업=오렌지 / 동작설명=중립 / 전략=블루(`--color-info`) / 쿨다운=바이올렛(`--color-cool`).
섹션 타이틀 옆 작은 정사각형 + 라벨 텍스트 색으로 표현(배경 풀필 없음, side-stripe 보더 없음 — 5차
수정 참고).

## 화면 간 공유 사항

- 팔레트, 타이포, 버튼 시스템, 반경 토큰, 모션 토큰, 가이드 섹션 보더 색, 스윕 언더라인.
- 앱 아이덴티티 바(브루탈 슬랩 보이스 — 두꺼운 하단 보더 + 올캡스 트래킹 워드마크).

## 화면별로 달라도 되는 것 (변경 없음)

관리자 화면은 로그아웃/세션 추가 버튼 + 상세 시트 안 관리 액션 행 + 기록 입력 섹션이 추가된다.

## Exports

Tailwind/DTCG/shadcn 매핑은 이 프로젝트에 해당 스택이 없어 생략한다. 토큰은
`public/styles.css`의 `:root` 블록이 단일 소스다.
